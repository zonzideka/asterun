# 开发规划

[中文](development-plan.md) | [English](en/development-plan.md)

首版以 `0.1.0a13` 已实现的能力为范围，后续工作集中在现有缺陷、兼容性、现场验收和发行文档。核心与三个可选插件采用 Apache-2.0。功能与支持范围见[发行说明](release-v0.1.md)，固定版本的测试和部署结果见 [STATUS](../STATUS.md)。

## 当前维护顺序

先核对当前提交、工作区改动和相关任务记录，再复现问题。保留已正确实现的协议、原生会话、权限、恢复和质量语义，按实际差量修改。默认验证使用隔离目录和 fake 后端；需要真实账户、部署或外部应用时，按该项任务约定执行。

| 工作 | 完成依据 |
|---|---|
| 调用方操作验收 | 从实际入口提交任务、观察至终态，并确认结果到达用户 |
| 外部主控维护候选 | 紧凑观察与游标保持、只读用量、固定外部报告及同任务有限修复的兼容和故障回归；部署后另做现场验收 |
| Codex 项目显示 | 核对执行目录、Git worktree、项目登记回执及原生客户端中的同一线程 |
| 审批体验 | 验证固定读取和已配置策略的放行范围，确认越界请求仍进入对应处理 |
| 发行文件 | 干净构建、sdist 重建、离线安装、许可与内容检查、手册样例 |
| 现有缺陷 | 固定复现、修改、相关回归与明确的兼容说明 |

工作区绑定、固定读取、桌面项目登记、结构化审查状态和收件箱消费者均已有实现。新的维护任务从这些入口复核；具体现场缺口见 [STATUS](../STATUS.md)。Windows、跨主机调度、多租户服务、Web 管理台、公共 HTTP MCP、自动付费路由和市场分发保留为后续候选。

当前[外部主控维护候选](caller-orchestration.md)尚未部署，保持既有观察默认值、权限和修复上限。回归须覆盖旧报告/目标漂移、固定文件检查超出 `target_paths`、`external_require_review` 跨 evaluate/repair 保留、同键异指令冲突及未知结果不重派。外部报告只记自报来源，同任务不充当原生续接证据；运行用量、主控开销与相同质量门下的节省率分开记录。

## 已交付工作索引

下表记录仓库实现。真实账户、续接、取消、客户端显示和模型质量按后端及固定版本另行验收。

| 工作项 | 已交付内容 | 依据 |
|---|---|---|
| A0-01 / A0-02 | 开发基线、旧源码筛选交接与哈希核验 | [环境](environment-baseline.md)、[来源说明](source-provenance.md) |
| A1-01 / A1-02 | Python 包、通用配置、应用契约与 fake 后端 | [安装](install.md)、[设计](design.md) |
| A1-03 | SQLite、单实例执行、操作意图、幂等和统一入口校验 | [ADR 0002](adr/0002-sqlite-and-single-instance.md) |
| A1-04 | Codex App Server 适配与显式启用 | [Codex 配置](install.md) |
| A2-01 | 会话绑定、本地索引、配置 CAS、原生引用及 Codex 续接 | [会话与配置](install.md) |
| A2-02 | 常驻观察、对账、取消、审批和编排接管 | [恢复操作](install.md)、[ADR 0009](adr/0009-resident-native-lifecycle.md) |
| A2-03 | Grok ACP 和 Claude CLI 适配 | [后端配置](install.md) |
| A3-01 / A3-02 | 固定版本质量流程、独立审查、有限修复和有界调度 | [ADR 0010](adr/0010-version-bound-quality-workflow.md)、[调优规划](tuning-plan.md) |
| A4-01 / A4-02 | 发行检查、备份、诊断、非代码样例和入口开关 | [安装](install.md)、[非代码样例](../examples/non-code/README.md) |
| RC-01 | 本机 runner-control/v1 实体、受理事务、事件、CAS、turns 账本与 outbox | [协议实现表](protocol/IMPLEMENTATION.md) |
| M1 P00–P05 | 基线复核、插件注册、配置 v2、固定计划、共享池、worker 和 Antigravity 插件 | [插件说明](plugins.md) |
| M2 P06–P08 | Cursor、Jules 独立插件及 capability profile 的执行和代码质量流程 | [插件说明](plugins.md)、[profile](capability-profile.md) |

早期 agent-runner 迁移材料由维护者单独归档。当前运行代码位于 `src/asterun/`，独立插件位于 `packages/`，源码范围及可复现核验方式见[来源说明](source-provenance.md)。

## 修改时保留的契约

配置 v1/v2、SQLite schema 6、冻结 runner-control/v1 和独立 capability profile 各自管理兼容。旧 Task/Run 与标准实体保持各自含义。接口调整应写清输入、返回、旧调用方式和恢复步骤，具体字段以 schema 与当前代码为准。

| 场景 | 检查要求 |
|---|---|
| 身份与资源 | 使用入口建立的主体，执行前核对工作区、动作、账户、运行主机、授权期限和配置 revision |
| 可靠派发 | 先持久保存意图；同键同输入复用原操作，同键异输入返回冲突，落库失败时派发计数为零 |
| 未知结果 | 使用原生引用对账，保留未知状态与预算占位，后续继续使用原操作关系 |
| 会话恢复 | 核对账户、主机、工作区、原生最新轮次和待处理审批 |
| 取消与接管 | 记录请求与后端终止证据；停止后续自动步骤后，再按安全边界交接 |
| 审批 | 绑定实际后端请求、运行、目标和操作摘要；拒绝过期、重复或错位回复 |
| 质量验收 | 检查和 finding 绑定目标版本，独立记录运行、验收、门禁与发布结果 |
| 并发与预算 | 保持唯一编排所有者、有限队列、共享池预留和修复上限 |
| 发布与备份 | 包内运行文件与源码一致；升级前保留一致快照，回退使用对应版本的副本 |

测试、构建和安装依赖属于代码执行。有效授权按原范围复用，新增接收方、费用路径或资源范围时重新匹配。供应商原生权限和操作系统隔离分别验证。

## 提交与验证

每项提交说明具体变更、实际命令和结果、兼容处理及未验证项。测试按纯逻辑、临时目录与协议替身、真实后端、原生客户端四层记录；启动进程、执行项目代码和消费额度的情况一并注明。

默认入口为 `scripts/verify-offline.sh`。针对局部缺陷先运行相关回归，发行内容变更检查 wheel/sdist 成员、正文链接、从 sdist 重建和安装结果。插件来源使用 `python3 packages/asterun-plugin-antigravity/scripts/verify-source.py` 核对。

新旧实例切换单独记录任务归属、未决操作、状态快照和回退步骤。调用方应用在各自仓库接线；涉及接口变化时，本仓先提供可复现的参数、返回和交接说明。
