# Asterun status

[中文](STATUS.md) | [English](STATUS.en.md)

The current version is `0.1.0a13`, licensed under Apache-2.0. The first release's feature scope is frozen. Maintenance focuses on defects, compatibility, live acceptance, and release materials. Start with the [README](README.en.md), [user manual](docs/en/user-manual.md), and [release notes](docs/en/release-v0.1.md).

The core provides CLI/MCP access, resident execution, task and native session references, permissions and approvals, idempotency, cancellation and reconciliation, scheduling, backup and restore, and optional code-quality workflows. Configuration v1/v2, SQLite schema 6, runner-control/v1, and the capability profile retain compatibility. Each Agent exposes its configured capabilities; callers organize their own task flows.

The [external caller workflow](docs/caller-orchestration.md) (Chinese) implements optional compact status, observation without event reads, read-only native usage reports by task/workspace/conversation, and `workflow-snapshot`, fixed external reports, and bounded repairs within one task. Existing interfaces and default observation behavior remain compatible: a watch deadline does not cancel execution, and skipping events does not advance their cursor. Live model execution, native clients, and net savings for this maintenance update require separate validation; usage reports are not verified bills.

External acceptance binds the current run, configuration revision, original task input, selected file versions, and report bytes. File checks in a fixed evaluation must also belong to `target_paths`. Reports retain the `external_reported` source and do not become independent review evidence. The persisted `external_require_review` requirement cannot be weakened by a later evaluate or repair. Repairs accumulate within the original task and its existing limits, without claiming native Grok continuation. The release snapshot and live records below retain their original validation scope.

On 2026-09-15, this maintenance update passed isolated `scripts/verify-offline.sh` validation on Python 3.14.0: 2205 passed and 4 skipped. The three pinned Antigravity source files and 34 packaging checks passed. That isolated validation covered offline protocol processes, SQLite reopening, and CLI/MCP entry points; no live model was invoked and no running instance was switched. Separate fake acceptance of the installed fixed wheel also passed: both CLI/MCP tasks succeeded, 94 schema records were valid, and restart replay and backup restoration passed. These results do not establish live backend acceptance.

On 2026-09-15, the local `default` instance was deployed with runtime commit `9862a3910cba0c614c110c5e254346687b5df9f6` and wheel SHA256 `16da70f3f8fd632b70a990e0bd2716ba26f7b160dc3c4fee8e33b50d79e84c47`. Configuration revision 5 and SQLite schema 6 were unchanged. The 20 tasks, 20 runs, 20 sessions, 1301 events, and 1445 review-artifact records matched before and after the switch. Production read-only acceptance passed CLI compact reads, observation without events, usage reports, and a snapshot manifest containing a missing file, plus MCP discovery of 47 tools, usage reports, and compact reads. No production task was added and no live model was invoked during this deployment. The previous installation and pre-switch backups are retained for rollback.

The initial public source snapshot is based on `c19535e`. Its 102 runtime files match `37bc705`, and all 102 test files are retained. The full offline suite on Python 3.11.16 completed with 2002 passed and 4 skipped, using temporary state and offline backend fixtures. The core and three plugins produced 4 packages and 8 wheel/sdist archives, with 34 packaging checks passed. All 4 sdists were rebuilt and all 4 wheels installed offline. The release notes describe existing live validation; see [source provenance](docs/en/source-provenance.md) for content digests and verification.

Before development, check the current commit, worktree, and related tasks. The default offline entry point is `scripts/verify-offline.sh`. See [design](docs/en/design.md), the [development plan](docs/en/development-plan.md), and the [protocol implementation](docs/en/protocol/IMPLEMENTATION.md).

The optional [GrokBot template](integrations/grokbot/README.en.md) has published `0.1.0-rc1`, with live acceptance continuing. It tracks ordinary tasks through a separate SQLite ledger and host routines. All 56 template offline tests, 34 core packaging checks, and 10 repeated concurrent-submission checks pass; core runtime code and existing protocols are unchanged. The current account has verified cloud installation and a fake task's routine wake after navigating away from the chat, delivery to the original chat, acknowledgement, and routine pause. Local Grok Build has completed a text task in the real project directory, and the summary has been checked against its input. The host receipt records host_reported_delivered, and the final poll requires no follow-up. Subsequent native UI checks confirmed that the local result and receipt match and that all three temporary routines are paused. The fixed ZIP passed anonymous-download and manifest checks. Its documentation retains the acceptance state at release; the current documents add later live confirmations. Version 3 of the [native Asterun template](https://x.ai/bot/4r3ysO1eA-6ZLI6cRoTNU) is published. All 11 memory entries and 3 skills were checked in full, followed by separate checks of the published card and public page. Same-account import has not been performed; clicking Add to Grok Bot requires the user to confirm the [sharing terms](https://x.ai/legal/bot-sharing-terms) at that step. Cloud model invocation, application restart, and independent new-user acceptance remain pending. See the [template acceptance record](integrations/grokbot/acceptance.md).
